import React from 'react'

const Cottage = (props)=> {
    const {cottageName, rate, blurb, guests, bedrooms, beds} = props

    return (
        <div>
            <h2>{cottageName}</h2>
        </div>
    )

    
}